<br>
<img src="https://graph.facebook.com/<%= @user.uid %>/picture?type=large" alt="some_text">
<br>
<br>
<%= form_for @user, url: {action: "update"}   do |f| %>
  <%= f.label :birthday, 'Birthday' %>:
  <%= f.date_select(:birthday, :order => [:month, :day, :year]) %><br />
  <%= f.label :sex_preference, 'Sexual Preference' %>:
  <%= f.select :sex_preference, [['Men','m'],['Women','f'],['Both','both']] %><br /> 
  <%= f.label :tagline, 'Tagline' %>:
   <%= f.text_field :tagline %><br /> 
  <%= f.submit "Update Profile" %>
<% end %>
